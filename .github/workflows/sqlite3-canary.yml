name: SQLite3 Security Canary

on:
  push:
    branches:
      - agenttube-analyst
    paths:
      - .github/workflows/sqlite3-canary.yml

permissions:
  contents: write

jobs:
  canary:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agenttube-analyst
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install current dependencies
        run: npm ci

      - name: Upgrade sqlite3 canary
        run: npm install sqlite3@^6.0.1 --save

      - name: Lint
        run: npm run lint

      - name: Analyst tests
        run: npm run test:analyst

      - name: Upstream AgentTube tests
        run: npm test

      - name: Critical runtime audit
        run: npm audit --omit=dev --audit-level=critical

      - name: Commit sqlite3 upgrade
        shell: bash
        run: |
          rm -f .github/workflows/sqlite3-canary.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json .github/workflows/sqlite3-canary.yml
          git commit -m "chore: upgrade sqlite3 after security canary"
          git push origin HEAD:agenttube-analyst
